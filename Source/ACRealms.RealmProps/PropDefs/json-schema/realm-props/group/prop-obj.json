{
  "$ref": "../common/prop-base.json",
  "description": "An object representing the realm property definition's attributes",
  "allOf": [
    {
      "$corvusTypeName": "ExtendedUntypedAttrs",
      "properties": {
        "description": {
          "$ref": "../../components/description.json",
          "description": "The description of the property, or the short description if the containing group defines description_format" } } }
  ]
}