{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$comment": "list of string fragments to be concatenated together to allow json string to be broken up into multiple lines in the editor",
  "type": "array", "maxItems": 320,
  "default": [ ],
  "items": { "type": "string", "minLength": 1, "maxLength": 200 }
}